<Page x:Class="NTAC_db.GUI.SettingsPages.AccesibilityPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="500" d:DesignWidth="600"
      Title="AccesibilityPage">

    <Grid>
        <Grid.Resources>
            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="Foreground" Value="{DynamicResource SettingsText}"/>
                <Setter Property="FontFamily" Value="Sitka Text"/>
                <Setter Property="FontSize" Value="14"/>
            </Style>
        </Grid.Resources>

        <TextBlock Text="Tema oscuro" Margin="40, 60" HorizontalAlignment="Left" VerticalAlignment="Top"/>

        <CheckBox x:Name="themeSwitch" IsChecked="{Binding darkTheme}" Margin="140, 60" HorizontalAlignment="Right" VerticalAlignment="Top"/>
    </Grid>
</Page>
